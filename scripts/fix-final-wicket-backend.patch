diff --git a/src/lib/scoring.ts b/src/lib/scoring.ts
--- a/src/lib/scoring.ts
+++ b/src/lib/scoring.ts
@@ -185,7 +185,12 @@ export async function recordPersistentDelivery(
   const bowlingIds = new Set(
     bowlingPlayers.map(
       (player) => player.playerId,
     ),
   );
+
+  const finalWicket =
+    input.isWicket === true &&
+    innings.wickets + 1 >= battingPlayers.length - 1;
 
   if (!battingIds.has(input.strikerId)) {
     throw new Error(
@@ -225,7 +230,7 @@ export async function recordPersistentDelivery(
       );
     }
 
-    if (!input.replacementPlayerId) {
+    if (!input.replacementPlayerId && !finalWicket) {
       throw new Error(
         "A wicket requires a replacement player.",
       );
@@ -243,57 +248,59 @@ export async function recordPersistentDelivery(
       );
     }
 
-    if (
-      !battingIds.has(
-        input.replacementPlayerId,
-      )
-    ) {
-      throw new Error(
-        "Replacement player is not a match player for the batting team.",
-      );
-    }
-
-    if (
-      input.replacementPlayerId ===
-      input.dismissedPlayerId
-    ) {
-      throw new Error(
-        "Replacement player cannot be the dismissed player.",
-      );
-    }
-
-    if (
-      input.replacementPlayerId ===
-        input.strikerId ||
-      input.replacementPlayerId ===
-        input.nonStrikerId
-    ) {
-      throw new Error(
-        "Replacement player is already on the field.",
-      );
-    }
-
-    /*
-     * A player already dismissed in this innings
-     * cannot return as the replacement.
-     */
-    const previousDismissal =
-      await prisma.wicket.findFirst({
-        where: {
-          dismissedPlayerId:
-            input.replacementPlayerId,
-          delivery: {
-            inningsId:
-              input.inningsId,
-          },
-        },
-      });
-
-    if (previousDismissal) {
-      throw new Error(
-        "Replacement player has already been dismissed in this innings.",
-      );
+    if (input.replacementPlayerId) {
+      if (
+        !battingIds.has(
+          input.replacementPlayerId,
+        )
+      ) {
+        throw new Error(
+          "Replacement player is not a match player for the batting team.",
+        );
+      }
+
+      if (
+        input.replacementPlayerId ===
+        input.dismissedPlayerId
+      ) {
+        throw new Error(
+          "Replacement player cannot be the dismissed player.",
+        );
+      }
+
+      if (
+        input.replacementPlayerId ===
+          input.strikerId ||
+        input.replacementPlayerId ===
+          input.nonStrikerId
+      ) {
+        throw new Error(
+          "Replacement player is already on the field.",
+        );
+      }
+
+      /*
+       * A player already dismissed in this innings
+       * cannot return as the replacement.
+       */
+      const previousDismissal =
+        await prisma.wicket.findFirst({
+          where: {
+            dismissedPlayerId:
+              input.replacementPlayerId,
+            delivery: {
+              inningsId:
+                input.inningsId,
+            },
+          },
+        });
+
+      if (previousDismissal) {
+        throw new Error(
+          "Replacement player has already been dismissed in this innings.",
+        );
+      }
     }
   } else if (
@@ -469,8 +476,10 @@ export async function recordPersistentDelivery(
   const newLegalBalls =
     innings.legalBalls +
     (result.isLegal ? 1 : 0);
 
   const inningsComplete =
-    newLegalBalls >=
-    innings.match.oversPerInnings * 6;
+    finalWicket ||
+    newLegalBalls >=
+      innings.match.oversPerInnings * 6;
 
   const currentOverFirstBowler =
     overDeliveries.length > 0
