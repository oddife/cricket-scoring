"use client";

import { useEffect, useRef, useState } from "react";
import BroadcastTicker, { BottomSlot, TickerData, TickerTeam, TickerStyle } from "@/components/BroadcastTicker";
import styles from "./ticker.module.css";

type Player = { id: string; name: string }; type Team = TickerTeam & { players: { player: Player }[] }; type SavedResponse = { ticker: TickerData; teams: TickerTeam[] }; type LiveMatch = { id: string; matchNumber?: number | null; teamA: TickerTeam; teamB: TickerTeam; venue?: string | null; status: string }; type MatchDetail = LiveMatch & { tossWinner?: { name?: string | null } | null; tossDecision?: string | null; players: { player: Player; teamId: string }[]; innings: Array<{ status: string; totalRuns: number; wickets: number; legalBalls: number; target?: number | null; runRate?: number | null; economy?: number | null; leadDeficit?: { kind: "NONE" | "TRAIL" | "LEVEL" | "LEAD"; runs: number }; currentStrikerId?: string | null; currentNonStrikerId?: string | null; currentBowlerAId?: string | null; battingTeamId?: string | null; deliveries: Array<{ bowlerId: string; strikerId: string; runsBat: number; runsTotal: number; isLegal: boolean; extraType?: string | null; wicket?: { bowlerId?: string | null } | null }> }> };
type DisplayOptions = { score: boolean; overs: boolean; batsmen: boolean; bowler: boolean; lastSix: boolean; teamNames: boolean };
const DISPLAY_DEFAULTS: DisplayOptions = { score: true, overs: true, batsmen: true, bowler: true, lastSix: true, teamNames: true }; const STYLE_OPTIONS: { id: TickerStyle; name: string; description: string }[] = [{ id: "compact", name: "Compact TV", description: "Balanced broadcast strip" }, { id: "classic", name: "Classic", description: "Traditional score bar" }, { id: "scoreboard", name: "Scoreboard", description: "Score-first presentation" }, { id: "minimal", name: "Minimal", description: "Clean, lightweight strip" }, { id: "wide", name: "Wide", description: "Larger broadcast graphic" }];
const BOTTOM_OPTIONS: { value: BottomSlot; label: string }[] = [{ value: "none", label: "None" }, { value: "target", label: "Target" }, { value: "leadDeficit", label: "Lead / Deficit" }, { value: "runRate", label: "Run Rate" }, { value: "economy", label: "Economy" }, { value: "venue", label: "Venue" }, { value: "toss", label: "Toss" }, { value: "custom", label: "Custom text" }]; const DEFAULT_BOTTOM_SLOTS: BottomSlot[] = ["leadDeficit", "none", "runRate", "economy", "toss"];
const SIZE_OPTIONS = ["70", "80", "90", "100", "110", "125", "140", "160", "180", "200"];
const EMPTY: TickerData = { style: "compact", teamAId: "", teamBId: "", teamAName: "", teamBName: "", score: "0-0", overs: "0.0", target: "", leadDeficit: "", runRate: "", economy: "", batsman1: { name: "", runs: "0", balls: "0" }, batsman2: { name: "", runs: "0", balls: "0" }, bowler: { name: "", figures: "0-0" }, lastSix: ["0", "0", "0", "0", "0", "0"], toss: "", venue: "", status: "LIVE", display: { ...DISPLAY_DEFAULTS }, bottomSlots: DEFAULT_BOTTOM_SLOTS, customBottomText: ["", "", "", "", ""], font: "Arial", fontSize: "100", batsmanNameSize: "100", teamLeftNameSize: "100", teamRightNameSize: "100", scoreSize: "100", oversSize: "100", logoSize: "100", outputWidth: 1280, outputHeight: 100, bottomHeight: 30 };
const MATCH_STORAGE_KEY = "new-castle-cricket-ticker-match"; const THEME_STORAGE_KEY = "new-castle-cricket-ticker-theme"; const LIVE_REFRESH_MS = 2000;
function playerName(players: MatchDetail["players"], id?: string | null) { return players.find((entry) => entry.player.id === id)?.player.name ?? ""; } function deliveryLabel(delivery: MatchDetail["innings"][number]["deliveries"][number]) { if (delivery.wicket) return "W"; if (delivery.extraType === "WIDE") return "Wd"; if (delivery.extraType === "NO_BALL") return "Nb"; return String(delivery.runsTotal ?? 0); }
function snapshotFromMatch(match: MatchDetail): Partial<TickerData> { const innings = [...match.innings].reverse().find((item) => item.status === "LIVE") ?? [...match.innings].reverse()[0]; if (!innings) return {}; const deliveries = innings.deliveries ?? []; const strikerId = innings.currentStrikerId; const nonStrikerId = innings.currentNonStrikerId; const currentBowlerId = deliveries.length ? deliveries[deliveries.length - 1].bowlerId : innings.currentBowlerAId; const battingStats = (id?: string | null) => { const ds = deliveries.filter((d) => d.strikerId === id); return { name: playerName(match.players, id), runs: String(ds.reduce((sum, d) => sum + d.runsBat, 0)), balls: String(ds.filter((d) => d.isLegal).length) }; }; const bowlerDeliveries = deliveries.filter((d) => d.bowlerId === currentBowlerId); const legalBalls = bowlerDeliveries.filter((d) => d.isLegal).length; const bowlerOvers = `${Math.floor(legalBalls / 6)}.${legalBalls % 6}`; const bowlerRuns = bowlerDeliveries.reduce((sum, d) => sum + d.runsTotal, 0); const bowlerWickets = bowlerDeliveries.filter((d) => d.wicket?.bowlerId === currentBowlerId).length; const position = innings.leadDeficit; const leadDeficit = position?.kind === "LEAD" ? `LEAD ${position.runs}` : position?.kind === "TRAIL" ? `DEFICIT ${position.runs}` : position?.kind === "LEVEL" ? "LEVEL" : ""; const lastSix = deliveries.slice(-6).map(deliveryLabel); return { teamAId: match.teamA.id, teamBId: match.teamB.id, battingTeamId: innings.battingTeamId ?? undefined, score: `${innings.totalRuns}-${innings.wickets}`, overs: `${Math.floor(innings.legalBalls / 6)}.${innings.legalBalls % 6}`, target: innings.target == null ? "" : String(innings.target), leadDeficit, runRate: innings.runRate == null ? "" : Number(innings.runRate).toFixed(2), economy: innings.economy == null ? "" : Number(innings.economy).toFixed(2), batsman1: battingStats(strikerId), batsman2: battingStats(nonStrikerId), bowler: { name: playerName(match.players, currentBowlerId), figures: `${bowlerOvers}-${bowlerRuns}-${bowlerWickets}` }, lastSix: lastSix.length ? lastSix : EMPTY.lastSix, status: match.status || "LIVE" }; }
export default function BroadcastTickerPage() { const [ticker, setTicker] = useState<TickerData>(EMPTY); const [teams, setTeams] = useState<Team[]>([]); const [savedTeams, setSavedTeams] = useState<TickerTeam[]>([]); const [liveMatches, setLiveMatches] = useState<LiveMatch[]>([]); const [selectedMatchId, setSelectedMatchId] = useState(""); const [loading, setLoading] = useState(true); const [loadingMatch, setLoadingMatch] = useState(false); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(""); const [darkMode, setDarkMode] = useState(false); const previewRef = useRef<HTMLDivElement>(null); const [previewScale, setPreviewScale] = useState(1);
  useEffect(() => { const saved = window.localStorage.getItem(THEME_STORAGE_KEY); const dark = saved === "dark"; setDarkMode(dark); document.documentElement.dataset.tickerTheme = dark ? "dark" : ""; return () => { delete document.documentElement.dataset.tickerTheme; }; }, []); useEffect(() => { document.documentElement.dataset.tickerTheme = darkMode ? "dark" : ""; window.localStorage.setItem(THEME_STORAGE_KEY, darkMode ? "dark" : "light"); }, [darkMode]);
  async function loadMatchSnapshot(matchId: string, showMessage: boolean) { try { const response = await fetch(`/api/matches/${matchId}`, { cache: "no-store" }); const match: MatchDetail = await response.json(); if (!response.ok) throw new Error((match as unknown as { error?: string }).error || "Failed to load match."); const live = snapshotFromMatch(match); setTicker((current) => ({ ...current, ...live, toss: current.toss, venue: current.venue, teamLeftNameSize: current.teamLeftNameSize, teamRightNameSize: current.teamRightNameSize, scoreSize: current.scoreSize, oversSize: current.oversSize, batsmanNameSize: current.batsmanNameSize, logoSize: current.logoSize })); setTeams((current) => { const byId = new Map(current.map((team) => [team.id, team])); for (const team of [match.teamA, match.teamB]) { const existing = byId.get(team.id); byId.set(team.id, { ...team, players: existing?.players ?? match.players.filter((entry) => entry.teamId === team.id).map((entry) => ({ player: entry.player })) }); } return [...byId.values()]; }); if (showMessage) setMessage("Live match data loaded. It refreshes automatically every 2 seconds."); } catch (error) { if (showMessage) setMessage(error instanceof Error ? error.message : "Failed to load match."); } }
  useEffect(() => { let cancelled = false; Promise.all([fetch("/api/teams", { cache: "no-store" }).then((r) => r.json()), fetch("/api/broadcast-ticker", { cache: "no-store" }).then((r) => r.json()), fetch("/api/matches?status=LIVE", { cache: "no-store" }).then((r) => r.json())]).then(([teamData, tickerData, matchData]: [Team[], SavedResponse, LiveMatch[]]) => { if (cancelled) return; setTeams(Array.isArray(teamData) ? teamData : []); const savedTicker = tickerData?.ticker ? { ...EMPTY, ...tickerData.ticker, display: { ...DISPLAY_DEFAULTS, ...(tickerData.ticker.display ?? {}) }, bottomSlots: Array.isArray(tickerData.ticker.bottomSlots) && tickerData.ticker.bottomSlots.length === 5 ? tickerData.ticker.bottomSlots : DEFAULT_BOTTOM_SLOTS, customBottomText: Array.isArray(tickerData.ticker.customBottomText) && tickerData.ticker.customBottomText.length === 5 ? tickerData.ticker.customBottomText : EMPTY.customBottomText } : EMPTY; setTicker(savedTicker); setSavedTeams(tickerData?.teams ?? []); const matches = Array.isArray(matchData) ? matchData : []; setLiveMatches(matches); const storedId = window.localStorage.getItem(MATCH_STORAGE_KEY) || ""; const storedMatch = matches.find((m) => m.id === storedId); const inferredMatch = matches.find((m) => m.teamA.id === savedTicker.teamAId && m.teamB.id === savedTicker.teamBId); const initialMatch = storedMatch || inferredMatch; if (initialMatch) { setSelectedMatchId(initialMatch.id); window.localStorage.setItem(MATCH_STORAGE_KEY, initialMatch.id); void loadMatchSnapshot(initialMatch.id, false); } }).catch(() => setMessage("Failed to load ticker data.")).finally(() => { if (!cancelled) setLoading(false); }); return () => { cancelled = true; }; }, []); useEffect(() => { if (!selectedMatchId) return; const timer = window.setInterval(() => { void loadMatchSnapshot(selectedMatchId, false); }, LIVE_REFRESH_MS); return () => window.clearInterval(timer); }, [selectedMatchId]);
  useEffect(() => { const updatePreviewScale = () => { const frame = previewRef.current; if (!frame) return; const width = Math.max(1, Number(ticker.outputWidth ?? 1280)); const height = Math.max(1, Number(ticker.outputHeight ?? 100)); const availableWidth = Math.max(1, frame.clientWidth - 20); const availableHeight = Math.max(1, frame.clientHeight - 20); setPreviewScale(Math.min(1, availableWidth / width, availableHeight / height)); }; updatePreviewScale(); const observer = new ResizeObserver(updatePreviewScale); if (previewRef.current) observer.observe(previewRef.current); window.addEventListener("resize", updatePreviewScale); return () => { observer.disconnect(); window.removeEventListener("resize", updatePreviewScale); }; }, [ticker.outputWidth, ticker.outputHeight]);
  const teamA = teams.find((team) => team.id === ticker.teamAId); const teamB = teams.find((team) => team.id === ticker.teamBId); const previewTeams = ([teamA, teamB].filter(Boolean) as TickerTeam[]).length ? [teamA, teamB].filter(Boolean) as TickerTeam[] : savedTeams; function update<K extends keyof TickerData>(key: K, value: TickerData[K]) { setTicker((current) => ({ ...current, [key]: value })); } function updateBottomSlot(index: number, value: BottomSlot) { setTicker((current) => { const slots = [...(current.bottomSlots ?? DEFAULT_BOTTOM_SLOTS)] as BottomSlot[]; slots[index] = value; return { ...current, bottomSlots: slots }; }); } function updateCustomBottomText(index: number, value: string) { setTicker((current) => { const text = [...(current.customBottomText ?? ["", "", "", "", ""])]; text[index] = value; return { ...current, customBottomText: text }; }); } function toggleDisplay(key: keyof DisplayOptions) { setTicker((current) => ({ ...current, display: { ...DISPLAY_DEFAULTS, ...(current.display ?? {}), [key]: !current.display?.[key] } })); }
  async function loadMatch(matchId: string) { setSelectedMatchId(matchId); if (matchId) window.localStorage.setItem(MATCH_STORAGE_KEY, matchId); else window.localStorage.removeItem(MATCH_STORAGE_KEY); if (!matchId) return; setLoadingMatch(true); setMessage(""); try { await loadMatchSnapshot(matchId, true); } finally { setLoadingMatch(false); } } async function save() { setSaving(true); setMessage(""); try { const response = await fetch("/api/broadcast-ticker", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(ticker) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Failed to save ticker."); setTicker((current) => ({ ...current, ...data.ticker })); setSavedTeams(previewTeams); setMessage("Ticker saved."); } catch (error) { setMessage(error instanceof Error ? error.message : "Failed to save ticker."); } finally { setSaving(false); } }
  if (loading) return <main className={styles.page}><div className={styles.loading}>Loading broadcast ticker…</div></main>; const previewWidth = Number(ticker.outputWidth ?? 1280) * previewScale; const previewHeight = Number(ticker.outputHeight ?? 100) * previewScale; const slotNames = ["Column 1 — Team", "Column 2 — Score", "Column 3 — Batsmen", "Column 4 — Bowler", "Column 5 — Team"];
  return <main className={styles.page}><div className={styles.header}><div><p className={styles.kicker}>BROADCAST GRAPHICS</p><h1>Compact Cricket Ticker</h1><p>Select a live match, choose the graphic style and set the ticker size.</p></div><div className={styles.actions}><button type="button" onClick={() => setDarkMode((value) => !value)}>{darkMode ? "☀ Light" : "☾ Dark"}</button><button type="button" onClick={() => window.open("/ticker/display", "_blank", "noopener,noreferrer")}>Open Ticker</button><button className={styles.primary} onClick={save} disabled={saving}>{saving ? "Saving…" : "Save Ticker"}</button></div></div><div className={styles.layout}><section className={styles.editor}>
    <div className={styles.section}><h2>Live match source</h2><label className={styles.fieldLabel}>Select live match<select className={styles.fieldControl} value={selectedMatchId} onChange={(event) => void loadMatch(event.target.value)} disabled={loadingMatch}><option value="">Choose a live match</option>{liveMatches.map((match) => <option key={match.id} value={match.id}>{match.matchNumber ? `Match ${match.matchNumber}: ` : ""}{match.teamA.shortName || match.teamA.name} vs {match.teamB.shortName || match.teamB.name}</option>)}</select></label><p className={styles.hint}>{liveMatches.length ? `${liveMatches.length} live match${liveMatches.length === 1 ? "" : "es"} available.` : "No live matches are currently available."}</p></div>
    <div className={styles.section}><h2>Design styles</h2><div className={styles.styleGallery}>{STYLE_OPTIONS.map((option) => <button type="button" key={option.id} className={`${styles.styleCard} ${ticker.style === option.id ? styles.styleCardSelected : ""}`} onClick={() => update("style", option.id)}><span className={`${styles.styleSwatch} ${styles[`swatch-${option.id}`]}`}><i></i><b></b><em></em></span><strong>{option.name}</strong><small>{option.description}</small></button>)}</div><div className={styles.grid2}><label className={styles.fieldLabel}>Font<select className={styles.fieldControl} value={ticker.font ?? "Arial"} onChange={(event) => update("font", event.target.value)}><option>Arial</option><option>Inter</option><option>Roboto</option><option>Helvetica</option></select></label><label className={styles.fieldLabel}>Overall scale<select className={styles.fieldControl} value={ticker.fontSize ?? "100"} onChange={(event) => update("fontSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label></div><h3>Element sizes</h3><div className={styles.grid2} style={{ gridTemplateColumns: "repeat(6, minmax(0, 1fr))" }}><label className={styles.fieldLabel}>Left team name<select className={styles.fieldControl} value={ticker.teamLeftNameSize ?? "100"} onChange={(event) => update("teamLeftNameSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label><label className={styles.fieldLabel}>Right team name<select className={styles.fieldControl} value={ticker.teamRightNameSize ?? "100"} onChange={(event) => update("teamRightNameSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label><label className={styles.fieldLabel}>Batsmen names<select className={styles.fieldControl} value={ticker.batsmanNameSize ?? "100"} onChange={(event) => update("batsmanNameSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label><label className={styles.fieldLabel}>Score<select className={styles.fieldControl} value={ticker.scoreSize ?? "100"} onChange={(event) => update("scoreSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label><label className={styles.fieldLabel}>Overs<select className={styles.fieldControl} value={ticker.oversSize ?? "100"} onChange={(event) => update("oversSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label><label className={styles.fieldLabel}>Logo size<select className={styles.fieldControl} value={ticker.logoSize ?? "100"} onChange={(event) => update("logoSize", event.target.value)}>{SIZE_OPTIONS.map((size) => <option key={size} value={size}>{size}%</option>)}</select></label></div></div>
    <div className={styles.section}><h2>Ticker size</h2><div className={styles.grid2}><label className={styles.fieldLabel}>Width (px)<input className={styles.fieldControl} type="number" min="640" max="7680" value={ticker.outputWidth ?? 1280} onChange={(event) => update("outputWidth", Number(event.target.value))} /></label><label className={styles.fieldLabel}>Height (px)<input className={styles.fieldControl} type="number" min="60" max="2160" value={ticker.outputHeight ?? 100} onChange={(event) => update("outputHeight", Number(event.target.value))} /></label></div><label className={styles.fieldLabel}>Bottom row height (px)<input className={styles.fieldControl} type="number" min="20" max={Math.max(20, Number(ticker.outputHeight ?? 100) - 20)} value={ticker.bottomHeight ?? 30} onChange={(event) => update("bottomHeight", Number(event.target.value))} /></label><p className={styles.hint}>Width and height define the exact output. Bottom row height is independent.</p></div>
    <div className={styles.section}><h2>Team display names</h2><div className={styles.grid2}><label className={styles.fieldLabel}>Team A display name<input className={styles.fieldControl} value={ticker.teamAName ?? ""} onChange={(event) => update("teamAName", event.target.value)} placeholder={teamA?.shortName || teamA?.name || "Auto short name"} maxLength={24} /></label><label className={styles.fieldLabel}>Team B display name<input className={styles.fieldControl} value={ticker.teamBName ?? ""} onChange={(event) => update("teamBName", event.target.value)} placeholder={teamB?.shortName || teamB?.name || "Auto short name"} maxLength={24} /></label></div><p className={styles.hint}>Leave blank for the automatic short name, or enter a custom broadcast name.</p></div>
    <div className={styles.section}><h2>Main ticker elements</h2><div className={styles.checkGrid}>{(Object.keys(DISPLAY_DEFAULTS) as (keyof DisplayOptions)[]).map((key) => <label className={styles.checkItem} key={key}><input className={styles.checkbox} type="checkbox" checked={ticker.display?.[key] ?? true} onChange={() => toggleDisplay(key)} /><span>{key === "lastSix" ? "Last 6 balls" : key === "teamNames" ? "Team names" : key[0].toUpperCase() + key.slice(1)}</span></label>)}</div></div>
    <div className={styles.section}><h2>Bottom row — 5 broadcast columns</h2><p className={styles.hint}>Each selector maps directly underneath the matching top section.</p><div className={styles.grid2}>{slotNames.map((name, index) => { const slot = (ticker.bottomSlots ?? DEFAULT_BOTTOM_SLOTS)[index] ?? "none"; return <div key={name}><label className={styles.fieldLabel}>{name}<select className={styles.fieldControl} value={slot} onChange={(event) => updateBottomSlot(index, event.target.value as BottomSlot)}>{BOTTOM_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>{slot === "custom" && <label className={styles.fieldLabel}>Custom text<input className={styles.fieldControl} value={(ticker.customBottomText ?? ["", "", "", "", ""])[index] ?? ""} onChange={(event) => updateCustomBottomText(index, event.target.value)} placeholder="Enter broadcast text" maxLength={80} /></label>}</div>; })}</div></div>
    <div className={styles.section}><h2>Automatic match data</h2><div className={styles.autoGrid}><div><span>Score</span><strong>{ticker.score}</strong></div><div><span>Overs</span><strong>{ticker.overs}</strong></div><div><span>Target</span><strong>{ticker.target || "—"}</strong></div><div><span>Lead / deficit</span><strong>{ticker.leadDeficit || "—"}</strong></div><div><span>Run rate</span><strong>{ticker.runRate || "—"}</strong></div><div><span>Economy</span><strong>{ticker.economy || "—"}</strong></div><div><span>Batsman 1</span><strong>{ticker.batsman1.name || "—"}</strong><small>{ticker.batsman1.runs} ({ticker.batsman1.balls})</small></div><div><span>Batsman 2</span><strong>{ticker.batsman2.name || "—"}</strong><small>{ticker.batsman2.runs} ({ticker.batsman2.balls})</small></div><div><span>Bowler</span><strong>{ticker.bowler.name || "—"}</strong><small>{ticker.bowler.figures}</small></div></div><div className={styles.ballInputs}>{ticker.lastSix.map((ball, index) => <span className={styles.autoBall} key={index}>{ball}</span>)}</div><p className={styles.hint}>Live data refreshes automatically every 2 seconds while a match is selected.</p></div>
    <div className={styles.section}><h2>Broadcast text</h2><div className={styles.grid2}><label className={styles.fieldLabel}>Toss / label<input className={styles.fieldControl} value={ticker.toss} onChange={(event) => update("toss", event.target.value)} placeholder="TOSS" /></label><label className={styles.fieldLabel}>Venue / location<input className={styles.fieldControl} value={ticker.venue} onChange={(event) => update("venue", event.target.value)} placeholder="VENUE" /></label></div></div>
    {message && <div className={styles.message}>{message}</div>}</section><section className={styles.preview}><div className={styles.previewHead}><h2>Preview</h2><span>{ticker.outputWidth} × {ticker.outputHeight}</span></div><div ref={previewRef} className={styles.previewFrame}><div style={{ width: `${previewWidth}px`, height: `${previewHeight}px`, flex: "0 0 auto", position: "relative" }}><div style={{ width: `${ticker.outputWidth}px`, height: `${ticker.outputHeight}px`, transform: `scale(${previewScale})`, transformOrigin: "top left" }}><BroadcastTicker ticker={ticker} teams={previewTeams} /></div></div></div><p>Preview is scaled proportionally to fit this panel. The actual display uses the exact Width × Height above.</p></section></div></main>;
}
